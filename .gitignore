# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
.pnpm-debug.log*

# env files
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# AI tool configs / worktrees
.claude/worktrees/
.claude/settings.local.json
.claude/settings.local.json*

.vercel

# local-only: cross-agent handoff files. Auto-generated from raw Claude
# transcripts, so they embed verbatim user messages — keep them OFF this
# public repo. Delete these two lines if you decide to publish them.
/*_CONTINUE_FROM_*.md

# local-only: interneta.world staging exports + superseded v1 engine
# (kept on disk as reference material, not part of the app)
/import/
/reference/
/lib/data/
/components/portal-tool.tsx
