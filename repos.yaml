# repos.yaml v0 — see github.com/adamtpang/repo-protocol
repo: portal.voyage
is: A data-driven map for deciding where to live, given passport and field.
url: https://portal.voyage
cluster: legibility-engines
ranks: places

provides:
  - id: constraint-matcher
    what: rank options against personal constraints
    at: lib/score.ts

stack: [next]

kin:
  - repo: optimism.fun
    why: idea-level kin, not shared code — both rank entities by weighted axes plus hard-constraint filters (see ECOSYSTEM.md); optimism.fun ranks problems, this repo ranks places
  - repo: pokedex.life
    why: idea-level kin, not shared code — both rank entities by weighted axes plus hard-constraint filters (see ECOSYSTEM.md); pokedex.life ranks people, this repo ranks places

canon: ECOSYSTEM.md
